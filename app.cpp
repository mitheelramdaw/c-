#include <iostream>
#include <string>
using namespace std;

int main() {
    string name;
    int age;

    cout << "What's your name bafo ? : ";
    cin >> name;

    cout << "What's your age bafo? : ";
    cin >> age;

    if (age < 18)
    {
        cout << "The hell wrong with you, " << name << " go home monkey ass " << name;
    }
    else if (age < 34)
    {
        cout << "Go home bafo " << name << ", have kids to take care of and bills to pay, mtate " << name;
    }
    else
    {
        cout << "Go on then " << name << ", have some galoooosh you mtate " << name;
    }

    return 0;
}
